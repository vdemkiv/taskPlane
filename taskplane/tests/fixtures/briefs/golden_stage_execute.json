# GOLDEN — frozen stage-wave Task-dispatch payload (t4, R-0004 /
# contract:wave-workflow). The Task path is the MANDATORY byte-identical
# fallback and the only Codex path: any change to a stage dispatch payload
# fails CI's stage parity leg even when the workflow path still works.
# Regenerate ONLY for a deliberate shape change:
#     python3 taskplane/tests/fixtures/briefs/regen.py
# Scrub rules (stage_fixture.py docstring): env scrub (CODEX_*/
# TASKPLANE_MODEL_*/TASKPLANE_WORKFLOWS/CLAUDE_CODE_WORKFLOWS/
# TASKPLANE_TASK unset); <WS>/<STORE>/<PLUGIN> path tokens; unix times
# zeroed; git shas + graph fingerprints -> <SHA>; calendar dates ->
# <DATE>; sorted keys; indent=2.
{
  "held": [],
  "instruction": "Dispatch ONE governed subagent per wave entry, concurrently. Per task: (1) `git worktree add <worktree> -b tp/<task>` from the approved baseline; (2) `tp.py loop claim <task> --agent-workspace <worktree>` \u2014 activates THAT task's contract in THAT worktree, so the hook confines the agent mechanically; (3) the subagent builds inside its worktree (TDD, primed lenses, acceptance criteria); (4) it COMMITS its work in the worktree (`git add -A && git commit`) and runs `tp.py loop submit pass|fail --task <id>`. The orchestrator alone runs the matching `loop gate`. When the wave empties, `loop next` evaluates each built task; on evaluate PASS merge its branch (`git merge tp/<task>`) and remove the worktree. EXCEPTION \u2014 entries with merge_on_pass=false are A/B variants: do NOT merge them; when all variants pass, the loop pauses at the SELECTION gate and the human picks what ships.",
  "parallel": true,
  "step": "execute",
  "wave": [
    {
      "design": null,
      "knowledge": "Prior decisions relevant to this work (from the knowledge base):\n  [0001] Plan approved: stage wave fixture (accepted, <DATE>) \u2014 tags: plan-approval\nHonor these unless you have a concrete reason to supersede one.",
      "lenses": [
        {
          "checks": [
            "gitleaks",
            "semgrep --config auto",
            "dependency audit"
          ],
          "id": "security",
          "looks_for": "secrets, authz gaps, injection, unsafe input, vulnerable deps",
          "mode": "inline",
          "name": "Security",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [
            "lint",
            "typecheck",
            "jscpd (copy-paste/duplication)"
          ],
          "id": "code-quality",
          "looks_for": "clarity, error handling, dead code, naming, duplication",
          "mode": "inline",
          "name": "Code quality",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [
            "coverage"
          ],
          "id": "testability",
          "looks_for": "coverage of new paths, seams/mockability, hidden globals, non-determinism",
          "mode": "inline",
          "name": "Testability",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [],
          "effort": "light",
          "id": "architecture",
          "looks_for": "component/service decomposition, data flow & coupling, state & consistency, scaling & failure modes, tech-choice fit",
          "mode": "inline",
          "name": "System design & architecture",
          "reasons": [
            "baseline (system design is always on)"
          ],
          "tier": "deep"
        }
      ],
      "merge_on_pass": true,
      "requirement": null,
      "task": {
        "deps": [],
        "id": "t1",
        "scope": [
          "src/alpha/**"
        ],
        "tests": "true",
        "variant": null
      },
      "worktree": ".tp-work/t1"
    },
    {
      "design": null,
      "knowledge": "Prior decisions relevant to this work (from the knowledge base):\n  [0001] Plan approved: stage wave fixture (accepted, <DATE>) \u2014 tags: plan-approval\nHonor these unless you have a concrete reason to supersede one.",
      "lenses": [
        {
          "checks": [
            "gitleaks",
            "semgrep --config auto",
            "dependency audit"
          ],
          "id": "security",
          "looks_for": "secrets, authz gaps, injection, unsafe input, vulnerable deps",
          "mode": "inline",
          "name": "Security",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [
            "lint",
            "typecheck",
            "jscpd (copy-paste/duplication)"
          ],
          "id": "code-quality",
          "looks_for": "clarity, error handling, dead code, naming, duplication",
          "mode": "inline",
          "name": "Code quality",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [
            "coverage"
          ],
          "id": "testability",
          "looks_for": "coverage of new paths, seams/mockability, hidden globals, non-determinism",
          "mode": "inline",
          "name": "Testability",
          "reasons": [
            "baseline (any code change)"
          ],
          "tier": "deep"
        },
        {
          "checks": [],
          "effort": "light",
          "id": "architecture",
          "looks_for": "component/service decomposition, data flow & coupling, state & consistency, scaling & failure modes, tech-choice fit",
          "mode": "inline",
          "name": "System design & architecture",
          "reasons": [
            "baseline (system design is always on)"
          ],
          "tier": "deep"
        }
      ],
      "merge_on_pass": true,
      "requirement": null,
      "task": {
        "deps": [],
        "id": "t2",
        "scope": [
          "src/beta/**"
        ],
        "tests": "true",
        "variant": null
      },
      "worktree": ".tp-work/t2"
    }
  ]
}
