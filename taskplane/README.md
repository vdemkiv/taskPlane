# Taskplane runtime

One advisory flow serves every role. See [CLI commands](../docs/cli-reference.md)
and [the shared flow](../skills/tp-go/references/shared-flow.md).

Run `python3 scripts/ci_local.py` for the complete non-browser suite, quality checks,
and both host packages. Add `--browser` to include the real-browser dashboard tests.
