{
 "schema": "taskplane.eval-scenario/v1",
 "skill": "fixture",
 "steps": [
  {
   "id": "N1",
   "claim": "every brief that was dispatched came back with its own findings file",
   "record": "dispatch",
   "check": "pairs",
   "select": {},
   "with": {
    "record": "context",
    "select": {
     "kind": "lens_findings"
    }
   },
   "key": "lens",
   "required": true
  },
  {
   "id": "C1",
   "claim": "the contract was active before the run first touched the workspace \u2014 the control row, so a red fixture is red for the reason it names",
   "record": "trace",
   "check": "before",
   "select": {
    "event": "contract_activated"
   },
   "before": "first_write",
   "universal": [
    "contract"
   ],
   "required": true
  }
 ]
}
