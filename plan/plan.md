# R-0013 Plan — native delivery authority and exact-SHA terminal truth

## Authority, baseline, and bounded impact

This bounded Plan action is anchored at exact source `5f5b49844c99f8a9982b6a4a6308fa04fc5bb5d1` on `codex/r0013-fresh-build-c9ec`, whose latest-main implementation parent remains `c9ec81a021ac74b048bfa58abfbfec870e49711a`. Product spec SHA-256 remains `e8d984c54e0900643f68d13d88d87f6f2fe6659ef84956519627a27afa12b3ed`. The retained approved Design fingerprint is `828833b7a08f47b769c801505706659de6d3e10bc9b39c7831e2a84062ef4dc2`, its approved baseline graph is `78b99071f8ebf44529afe2d70497557d4b280f12f63faf60208927ada466c7f4`, and the current `design/contract.json` has SHA-256 `c218c69533146144017b4dfbb5a701512e774e737c836c3e925c0792fa3019d6`.

Volodymyr Demkiv first authorized the locator repair, `2.17.23` build/push/install, and genuine native-host proof before the remaining R-0013 Build. Live `2.17.23` then proved a second, distinct blocker: `loop._collect_zero_lens_evaluate_before_guidance` raises the W31 refusal unconditionally before it can consume any genuine producer observation. Volodymyr explicitly approved the narrow correction: deliver the smallest durable Codex producer adapter first, synchronize it as `2.17.24`, build and push only the current branch, install that exact version, prove the real path live, and then continue R-0013. These attributed decisions narrowly override the stale R-0013 exclusions only for this prerequisite. They do not change the seven Product outcomes, add a contract id, replay Product or Design, broaden W31 or cold-start scope, or authorize a tag, public Marketplace publication, release declaration, `origin/main` mutation, or any other remote mutation.

The exactly one bounded impact derivation covered the locator and hook sources, host event/capability ports, producer-observation policy, zero-lens collection and submission adapters, synchronized Codex and Claude manifests, compatibility and release-evidence surfaces, OpenAI packager, public version records, and affected tests. It touched `(root)`, `.claude-plugin`, `.codex`, `.codex-plugin`, `design`, `design/schemas`, `hooks`, `scripts`, `taskplane`, and `taskplane/tests`; found 24 impacted nodes including `req:R-0013`; had no truncation, policy block, unknown module, or degraded scanner. The generated `.taskplane` launcher remains an explicit T00 runtime surface even though generated checkout files are not graph-scanned. The impact graph fingerprint is `1eaf1a9ea8c77920a869ed6bbf3651ebbcf2d758d52a351ecd13ba9084aeba41` and the scan-quality fingerprint is `f4ad0c6aaf690247e247cf549a687c3741510a855c4205a792d1bec6c570e2af`. The bounded command ran from target HEAD `5f5b49844c99f8a9982b6a4a6308fa04fc5bb5d1`, while its complete cached module graph reports scanned revision `3039b952c3be5ad340bf65804fede95a69fcbd30`. The 21 intervening source changes remain inside already named T00 modules and add no component boundary, but this revision mismatch is an explicit Plan-validation risk and may not be represented as exact-tip graph proof. The policy remains local depth 3, contract-only boundary, contract depth 1, and requirement depth 1.

## Authorized T00 correction and release boundary

T00 remains the serialized prerequisite and is not complete merely when source tests pass. Its locator half binds native-hook and repository-bridge execution in both root and Taskplane task worktrees to the canonical home selected by the secure checkout locator. A missing locator, a non-canonical locator home, a task-worktree locator that does not bind the root run/home, or an inherited home that disagrees with the locator must fail closed before any event, capability, observation, or receipt write. An explicit secure locator that selects canonical `$HOME/.taskplane` remains valid; the defect is unauthorized fallback or mismatch. The generated launcher and host runtime must preserve the locator-bound home, and receipt copying is prohibited as implementation, fixture, fallback, or acceptance evidence.

The producer half closes the observed production gap without creating Taskplane lifecycle authority. On a genuine Codex `SubagentStop`, the hook must observe the evaluator or execution-time EM result's exact bytes and bind exactly one external event plus one-use capability to the current run, task, lowercase stage, emitted producer identity, Codex session, Codex turn, canonical result path, output schema id, output-contract fingerprint, and exact source SHA. Zero-lens submission must locate and consume exactly one fresh matching observation before collection; missing, stale, ambiguous, mismatched, replayed, caller-authored, copied, or synthetic material fails closed before guidance, collection, submission, or gate success. A duplicate native/bridge delivery may replay protocol disposition only and must never mint a second event or capability. This is a host evidence producer/consumer adapter, not a scheduler, worker registry, lifecycle journal, queue, or general W31/cold-start redesign.

The same T00 task commit must synchronize `2.17.24` across authoritative Codex and Claude manifests, Marketplace metadata, runtime release evidence, compatibility/schema/CI assertions, README and CHANGELOG; retain `2.17.23` and older versions as history; and keep one shell-free declared pytest command as the engine gate. `python3 scripts/ci_evals.py --verify-release-surface` and `python3 scripts/package_openai.py` remain separate mandatory orchestrator evidence. After independent source evaluation, the exact candidate crosses a root-owned boundary: push only `codex/r0013-fresh-build-c9ec`, prove the remote tip is that SHA, install `/Users/vdemkiv/.codex/plugins/cache/taskplane-marketplace/taskplane/2.17.24`, and verify its authoritative manifest is exactly `2.17.24`.

Finally, without copied receipts, a fresh exact-name Codex evaluator in this checkout/run must produce a current result and a genuine SubagentStart/SubagentStop lifecycle through installed `2.17.24`; an execution-time EM canary must exercise the same adapter contract. Evidence must prove `native_effective`, managed policy `permitted`, a loaded session, one stable fresh native identity, the current root/task checkout binding, run `a8308709d819466c9de50f63b2f90226`, the locator-bound `/Users/vdemkiv/.taskplane-r0013` home, exact output bytes/schema/contract/source SHA, a fresh external host event, successful one-use capability consumption, and replay refusal. No unauthorized default-home write may occur. Only one `bootstrap-home-ready` receipt containing the implementation SHA, shell-free suite result, separate release/package digests, exact pushed ref, installed-manifest digest, live lifecycle fingerprint, external-event fingerprint, producer-observation fingerprint, and consumed-capability fingerprint may pass T00 and make T01–T07 ready.

## Delivery DAG and receipts

There are exactly ten tasks: one serialized bootstrap/producer/version prerequisite followed by the unchanged three-wave R-0013 implementation, integration, and closure graph. T00 owns only the locator-bound host producer/consumer seam, synchronized patch version, package, current-branch push, local install, and genuine-host proof barrier described above. After its independent gate passes, Wave 1 offers the seven pairwise-disjoint leaf-readiness owners as one native set. Wave 2 remains the sole shared-adapter integration owner after all seven leaf receipts, and Wave 3 remains AC7 end-to-end closure after the integration receipt.

| Wave | Task | Exclusive owner | Receipt / barrier |
| --- | --- | --- | --- |
| Prerequisite | T00 bootstrap/producer/version | Root/task-worktree locator-home binding; genuine SubagentStop event plus one-use producer capability; exact zero-lens consumption/refusal; `2.17.24` synchronized surfaces; affected regressions; deterministic OpenAI package; exact branch push/install; genuine evaluator and EM canary proof | `bootstrap-home-ready`; source, output bytes/schema/contract/SHA, event, capability, observation, package, pushed SHA, installed manifest, and live-host evidence must agree |
| 1 | T01 AC1 | Native authority validator and exact tests | `leaf-ready:AC1` |
| 1 | T02 AC2 | Design-sweep validator and exact tests; validates the retained one-time Design evidence and never runs another lens sweep | `leaf-ready:AC2` |
| 1 | T03 AC3 | Zero-lens execution adapters and exact tests | `leaf-ready:AC3` |
| 1 | T04 AC4 | Sealed ready set, BUILD-C consumption, native intent and wait adapters | `leaf-ready:AC4` |
| 1 | T05 AC5 | Seven-outcome ceiling and exhaustive pair-map validation | `leaf-ready:AC5` |
| 1 | T06 AC6 | Delta handoff, native usage and hard budget screen | `leaf-ready:AC6` |
| 1 | T07 AC7 | Pure wiring/finalization APIs and focused fault/refusal selectors, not end-to-end acceptance | `leaf-ready:AC7` |
| 2 | T08 | Only `taskplane/loop.py` and `taskplane/tp.py`; remove stage-native execution authority and connect the seven sealed APIs | `integration-ready:r0013`; requires T01–T07 |
| 3 | T09 AC7 | Exact pinned/final checkout proofs, 21 producer-edge mutations, eight-surface reconciliation, full suite, and redacted terminal projection | `acceptance-complete:AC7`; requires T08 |

T00 intentionally overlaps T03 at `taskplane/evaluation_output.py`, T07 at `taskplane/release_evidence.py`, and T08 at `taskplane/loop.py` and `taskplane/tp.py`. Every leaf already depends on T00, and T08 depends on every leaf, so these are named prerequisite overlaps rather than same-wave conflicts; no downstream scope, selector, or dependency object changes. The seven Wave-1 leaf scopes remain pairwise disjoint. T01, T03, T04, and T06 retain leaf-only selectors; shared and live-path R-0013 selectors remain exclusively at T08, while T00 uses only focused bootstrap/producer selectors needed to make leaf Evaluate possible. No task owns host spawn, scheduling, capacity, reservation, admission, replay, lease-concurrency, worker lifecycle, event queue, or execution DAG. `delivery_mode=build` and `automatic_lenses=[]`; Build, Fix, Evaluate, and execution-time EM use zero Taskplane lens workers. T02 validates retained Design-only evidence and never dispatches or repeats the completed all-26 sweep.

## Seven outcomes, four P0 controls, and all 21 pair classifications

The unique criterion set remains exactly AC1–AC7, copied verbatim into `tasks.json`; T00 re-attests only AC1, AC3, and AC4 because its bounded adapter must preserve Codex authority, make a genuine zero-lens Evaluate/EM result collectible, and bind the emitted native identity. It creates no eighth outcome. T03 remains the leaf owner for the broader execution zero-lens policy, and assigning final-candidate AC7 to T00 remains impossible before T08 integration and T09 closure; AC7 stays fully assigned to T07/T09, with T08 carrying only its integration re-attestation. T00 completion requires the `2.17.24` source fix, shell-free suite, separate release/package evidence, exact branch push/install, and genuine live producer proof. The only P0 controls remain P0-1 atomic exact-SHA finalization (AC7), P0-2 native budget telemetry and enforcement (AC6), P0-3 host-native dispatch authority (AC1 and AC4), and P0-4 real-checkout wiring closure (AC7).

The Design's exact outcome-level pair policy remains authoritative. A serialized row is an acceptance/evidence dependency, not permission to idle disjoint leaf implementation: all seven leaf owners may build in Wave 1, while T08 and T09 enforce the named receipt barriers before the dependent acceptance can close.

| Pair | Classification | Available wave / exact reason |
| --- | --- | --- |
| AC1–AC2 | parallel | `leaf-wave-1`: disjoint native-authority and Design-sweep owners |
| AC1–AC3 | parallel | `leaf-wave-1`: disjoint native-authority and zero-lens owners |
| AC1–AC4 | serialized | AC4 adapter integration consumes the AC1 authority map and removed stage edge |
| AC1–AC5 | parallel | `leaf-wave-1`: disjoint native-authority and Plan-boundary owners |
| AC1–AC6 | parallel | `leaf-wave-1`: disjoint native-authority and budget owners |
| AC1–AC7 | serialized | AC7 finalization consumes the AC1 native-authority receipt |
| AC2–AC3 | parallel | `leaf-wave-1`: disjoint Design-sweep and zero-lens owners |
| AC2–AC4 | parallel | `leaf-wave-1`: Design-only sweep validation is independent of native dispatch leaf code |
| AC2–AC5 | parallel | `leaf-wave-1`: disjoint Design-sweep and Plan-boundary owners |
| AC2–AC6 | parallel | `leaf-wave-1`: disjoint Design-sweep and budget owners |
| AC2–AC7 | serialized | terminal acceptance closes only after the Design-sweep receipt is dispositioned |
| AC3–AC4 | serialized | AC4 native briefs consume AC3 zero-lens execution authorization at the shared loop adapter |
| AC3–AC5 | parallel | `leaf-wave-1`: disjoint zero-lens and Plan-boundary owners |
| AC3–AC6 | parallel | `leaf-wave-1`: disjoint zero-lens and budget owners |
| AC3–AC7 | serialized | AC7 finalization consumes execution zero-lens receipt and trace |
| AC4–AC5 | serialized | AC4 ready-set emission consumes AC5 exhaustive Plan pair map |
| AC4–AC6 | serialized | AC4 native start screen consumes AC6 non-null usage and budget decision |
| AC4–AC7 | serialized | AC7 finalization consumes native dispatch and wait receipts |
| AC5–AC6 | parallel | `leaf-wave-1`: disjoint Plan-boundary and budget owners |
| AC5–AC7 | serialized | AC7 finalization consumes exact seven-outcome Plan receipt |
| AC6–AC7 | serialized | AC7 finalization consumes complete native usage and bounded-handoff receipts |

## Contract, module, edge, and depth coverage

The task-set contract union remains exactly the nine canonical Product ids: `contract:design.codex-native-capability-inventory`, `contract:design.quick-concurrent-all-lens-sweep`, `contract:delivery.execution-zero-lens`, `contract:delivery.codex-native-dispatch`, `contract:delivery.event-driven-wait`, `contract:delivery.acceptance-wave-ceiling`, `contract:delivery.bounded-stage-handoff`, `contract:delivery.exact-sha-terminal-truth`, and `resource:exports.exact-sha-terminal-truth`. T00 uses only the existing `contract:delivery.execution-zero-lens` producer-consumption boundary and `contract:delivery.codex-native-dispatch` host-lifecycle observation boundary. Volodymyr Demkiv's attributed correction supplies authority for this prerequisite without adding or renaming a Product contract.

The retained terminal inventory still covers all 11 approved proposed modules and the previously planned `exports/terminal` projection. The bounded impact returned no unknown module. T00 declares only the generated `.taskplane` runtime module; `hooks`, `scripts`, plugin manifests, `design`, and `taskplane` are existing graph surfaces. T00 invents no Design edge and no new producer service. T09 retains the exact 24 approved edge strings, and every task retains local depth 3, contract-only boundary, contract depth 1, and requirement depth 1.

## Risks, budgets, exclusions, and stop conditions

The highest immediate risk is confusing policy/test-double coverage or a copied receipt with a production producer path. T00 must bind root and task checkout locators to one canonical home/run, observe exact result bytes only inside the real SubagentStop host event, mint one narrowly bound capability, consume it once, and durably reject replay. Missing/stale/ambiguous/mismatched/caller-authored/copied/synthetic evidence, duplicate hook-path minting, version drift, nondeterministic package, remote-tip mismatch, installed-manifest mismatch, cross-run identity inheritance, default-home leakage, unauthorized scope drift, or any Taskplane worker-lifecycle authority stops T00. The cached graph revision mismatch (`3039b95` graph versus `5f5b498` target) is also explicit: the intervening files add no module boundary, but the Plan gate must refuse if it requires exact-tip graph freshness. After T00, containment remains seven disjoint leaf receipts, one exclusive adapter integrator, AC7 only after integration, registered exact checkouts, mutation proofs, an orchestrator-only terminal capability, exactly eight immutable projections, and one bounded CAS reconciliation head.

Before every native start, elapsed time, unique sessions, total tokens, and uncached input must be finite, non-negative, and strictly below 28,800 seconds, 60 sessions, 150,000,000 tokens, and 25,000,000 uncached tokens. Equality, breach, missing usage, a handoff at or above 4,000 tokens, silent transport deadline, or a second failed Fix/Evaluate cycle stops for attributed human scope or architecture review. The stopped set is never silently resumed or replaced.

AC7 final truth is exactly eight projections—Git HEAD, governed progress, run journal, tasks/gates, public report, repository verification, release evidence, and redacted exports terminal evidence—sharing one SHA/status/requirement/fingerprint set. No individual projection, opaque fingerprint, generated temporary checkout, or foreign receipt has authority. A dirty/wrong SHA, missing selector, failed severed-edge proof, CAS fork/gap/collision, executing retained run, partial projection, cleanup before reconciliation, or SHA-changing merge blocks Done and every delivery claim.

This revision admits only the locator-bound bootstrap repair already present, the smallest genuine Codex producer adapter, `2.17.24` patch synchronization, deterministic OpenAI package, current-branch push, exact local install, and genuine live producer/consumer proof prerequisite. It does not admit broader W31/cold-start redesign, history repair, P1/P2 follow-up, R-0011, unrelated R-0013 backlog, completed R-0001 replay, scheduler/capacity/reservation/admission/replay/lease/lifecycle machinery, caller-authored or copied receipts, or another lens sweep. The only authorized remote boundary is root's push of the independently green T00 candidate to `codex/r0013-fresh-build-c9ec`; merge to main, tag, public Marketplace publication, release declaration, credentials for any other mutation, and all other origin changes remain excluded. Plan authoring runs no product tests or implementation. The orchestrator may submit this revision to the mechanical Plan gate, but no T00 implementation dispatch occurs before the validated Plan is human-approved, and no remaining R-0013 dispatch occurs before the complete `bootstrap-home-ready` evidence passes.
