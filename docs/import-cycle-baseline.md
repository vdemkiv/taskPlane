# Import-cycle continuation baseline

The import-cycle checker is unchanged. It still measures imports at every AST
depth, including `TYPE_CHECKING`, and rejects new cyclic members or internal
edges beyond the checked-in inventory. This is a fixed-revision debt baseline,
not a claim that the dependency graph is cycle-free or independently approved.

## Recorded measurements

| Source | Strongly connected components (members / internal edges) |
| --- | --- |
| Original wave `a45aae112bd5b6113292771204ca9ff867bf7ff8` | 17 / 49; 7 / 13 |
| Prior checked policy source `110e765ef58fcb7b16916063790b772bf773e175` | 17 / 49; 7 / 13 |
| User-selected accepted T19 `e2602749f17db2403550d124bcf055026f082730` | 70 / 342 |
| Fixed typing correction `fd5ff0cec8ca5267d0576f5c53289cc4ecb194c9` | 70 / 343 |

The user selected the latest accepted T19 as the continuation comparison
baseline. The original whole-wave baseline and its smaller historical inventory
remain explicit above. T19 acceptance is retained Build history; deferred reviews
are non-judged, not historical independent-review passes. The accumulated cycle
debt remains visible for final Engineering review.

The sole topology addition between the selected T19 and the fixed typing
correction is `taskplane.design_host_transport -> taskplane.review_evidence`.
It supplies the `ArtifactStore` annotation inside `TYPE_CHECKING`, not a runtime
import. There are no added cyclic members, removed edges, or other added internal
edges between those revisions. Under the user's delegated night-delivery
authority, the orchestrator explicitly retained this one static type dependency,
subject to final independent Engineering review. This is an attributed assistant
disposition, not a new human click, whole-wave approval, or review PASS.

`taskplane/tests/fixtures/import-cycles.json` is generated by the existing
`build_inventory_at_revision` and `canonical_json` owners from the full fixed
typing revision above, never from moving HEAD. Its `source_revision` names that
exact source. Current work must remain within that measured inventory; additional
members or edges are new findings, not automatically authorized debt. The
unchanged `check_inventory` continues to reject added-edge and new-member
mutations. No checker exception, import hiding, or runtime policy was introduced.
