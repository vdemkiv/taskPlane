# Live delivery runtime repair — 2026-09-15

## Result

The runtime defects are repaired, committed in an isolated worktree, packaged, and installed through the supported plugin update flow. A fresh native run verifies all four worker routes, output binding at Stop, truthful budget refusal, and immediate terminal cleanup.

**Full native delivery remains incomplete.** Product measured **2,285,008 total tokens** against its unchanged **100,000-token** limit. The run correctly refused progression and ended as an interruption. Design, Plan, Build, Evaluate, Engineering, and normal completion Retro did not run natively. No budget waiver, fabricated approval, or changed accounting definition was used.

## Source and installed build

- Worktree: `/private/tmp/taskplane-live-delivery-repair`.
- Branch: `codex/live-delivery-repair`, based on `998b0e4c3e35feb5862c6fa23c376e38f1a48743`.
- Runtime commits: `7bbeefae68d49f6677dd38d5775137ebcaaee5fd` and `dfa003b91462389a09407719e097dd5346936bfc`.
- Installed: `taskplane@personal`, version `2.23.1+codex.20260915164940`.
- Package: `/Users/vdemkiv/Documents/taskPlane/exports/taskplane-live-repair-dfa003b/taskplane-2.23.1-openai.zip`.
- Package SHA-256: `f4aaa3fe661b28a3ad92985680d6aecaa7abec29354f011f45cab114a22d13e2`.
- All 262 packaged files match the installed build after normalizing only the official manifest cachebuster suffix.

The registered source was backed up, updated from the verified package, validated, cachebusted with the official helper, and reinstalled using the supported Codex plugin command. The installed cache was not edited directly. No release or publication was performed. Source provenance is verified; it is not a claim of a release-green CI receipt.

The original source checkout remains on `codex/restore-2.23.1-harness-recovery` at `998b0e4c3e35feb5862c6fa23c376e38f1a48743`. The runtime changes have not been merged into that checkout. All 93 files in the original failure evidence manifest still match their hashes. Current project contract locations contain no active worker contracts; historical archives and diagnostic copies remain intact.

## Changes

1. **Validate Product drafts before review.** Startup describes the canonical closed requirement schema, and candidate admission uses that same schema before issuing specialist leases.
2. **Bind collection to the actual Stop.** Stop records hash the exact raw candidate bytes, including malformed drafts. Collection refuses changes after Stop. An explicitly authorized retry of a stopped, uncollected Product, Design, or Plan attempt grants a fresh slot, nonce, and worker identity through the existing retry journal. Previous evidence stays unaccepted.
3. **Read the correct native usage counter.** Usage selects the latest complete counter at or before the authenticated Stop within the existing bounded tail. Foreign lineage, resumed segments, missing counters, and corrupt metadata still fail closed. Terminal accounting closes independently of candidate acceptance.
4. **Clean up before claiming a clean terminal.** Whole-run termination retires its exact signed phase-worker slots before reporting cleanup. An active worker requires its authenticated Stop. Unrelated slots are retained. Session cleanup preserves interruption and cancellation outcomes.
5. **Authenticate native specialist routes.** Leases register exact expected task, model, reasoning, role, and isolated context. Reopening a consumed lease does not authorize another dispatch. When the host protects prompt text, role verification uses the original signed activation action and the same signature, freshness, and workspace checks as activation. It also verifies exact lease, worker, slot, and read-only output scope.

## Source verification

Regression tests cover invalid draft admission, changed output after Stop, explicit retry authority, immutable earlier evidence, fresh attempt identity, terminal accounting, immediate cleanup, missing-Stop refusal, and specialist dispatch identity. Protected-prompt tests reject unsigned, foreign, altered-scope, and corrupt-signature claims.

| Check partition | Observed result |
| --- | --- |
| Initial phase/identity/lifecycle regression partition | 190 passed before an Evaluate tool-environment failure |
| Corrected isolated environment plus final focused repairs | 6 passed |
| Amendment, native terminal telemetry, and recorder | 130 passed |
| Remaining stage integration, including later phases and closure | 8 passed |
| Final protected-role, lifecycle, and host-compatibility suites | 79 passed |

Counts overlap and must not be summed into a single suite total. Integration tests use simulated host events; they do not establish native delivery acceptance. Selected production lint and diff checks pass, as does strict typing of the four changed typed modules. Existing legacy typing overrides remain in place.

Two validation issues were reproduced without the production repair: an old continuity test expected obsolete onboarding prose, and Evaluate could not see tools installed only in Python's user site. The test now checks that continuation grants no approval. A temporary isolated environment was populated from already installed Ruff, mypy, Bandit, and dependencies, with no downloads or global installation changes. All four real quality probes pass there.

## Native verification

### Preserved original failure

Original run: `67c8b79169f543eeae879e51ac5d0b49`. A read-only recheck selects counter ordinal 215 at `2026-09-15T15:15:52.593Z`, before its authenticated Stop at `15:15:52.984843Z`. Usage was 1,771,137 input tokens (including 1,696,640 cached input) plus 7,847 output tokens: **1,778,984 total**. Its limit was 100,000. The formerly selected latest counter occurred 385 seconds after Stop. No historical receipt was changed.

### First repaired run

Run `a9d2a031217040a6959d8db6ff1e0703` loaded installed plugin build `2.23.1+codex.20260915163151` in `/private/tmp/taskplane-live-repair-20260915-01a0a595`. Product and three native specialists ran. The candidate matched its Stop hash, usage was **1,398,184 total tokens**, progression correctly refused with `phase_token_budget_exhausted`, and interruption immediately left zero active contracts and zero cleanup leaks.

This run exposed a remaining protected-prompt role check defect. Task, model, reasoning, and context matched, but specialist role checks failed. The signed-activation repair in `dfa003b` addresses that observed failure. Its historical audit was retained unchanged.

### Final repaired run

- Fixture: `/private/tmp/taskplane-live-repair-roles-20260915-01a0a595`.
- Baseline: `4f36df98a3fd4ff977643eb62a4a73165fb0211a`.
- Native orchestrator: `01a0a5fa-d6f2-76a0-8bbe-508d855899aa`.
- Run: `81ebb02ad05f46ca85368510aa5011a7`.
- Requirement: fresh R-0001 for the dependency-free interval-merging fixture.
- Native routing audit: **4 expected, 4 observed, zero mismatches, zero missing or dropped dispatches**. Product and all three specialists have exact routes verified, including protected-prompt roles.
- Product Stop: authenticated native receipt for worker `01a0a5fc-d06d-7021-b8ac-be779db3fa73` and its exact native turn.
- Candidate: `specs/requirement.json` still hashes to `3e880f2a427dd2a396609be80a2fce783978877b8dd6a079efe63d6761a30daf`, exactly matching Stop.
- Product usage: 2,277,572 input tokens, including 2,194,816 cached input, plus 7,436 output tokens = **2,285,008 total**. Reasoning is an output subcount, not added again.
- Collection: refused with `budget_exhausted` against 100,000 tokens. Three specialist results were collected, but Product itself was not accepted.
- Terminal: actual whole-run **interruption**, with the Product contract retired under that outcome. An immediate filesystem capture before any intervening host-tool or session cleanup found **zero active contracts**. Native cleanup reports **zero leaks**.

The ordinary post-closure README read succeeded with exit code 0. The final native process exited 0 after 944.076 seconds; that process exit reports successful observation and safe interruption, not feature delivery success.

**Runtime-path qualification:** both phase fixtures were initially onboarded from the repair worktree. Native installed hooks called the fixture launcher, which resolved `/private/tmp/taskplane-live-delivery-repair/taskplane/tp.py`. The final run therefore verifies the repaired runtime through native lifecycle events, but does not alone prove that phase commands resolve the installed cache path. A read-only comparison verifies 169 packaged runtime, agent, skill, and hook files byte-for-byte; the remaining hook manifest matches after the official OpenAI package projection. The worktree runtime was unchanged throughout the final run. After that run closed, the public installed `onboard --install-launcher` path corrected the fixture launcher. A separate fresh native session then resolved the exact installed cache engine, reported all 11 readiness checks passing with an actual current native-hook observation, and successfully read README. Its process exited 0 after 65.993 seconds. This verifies the corrected installed entry point and does not retroactively change the phase-run evidence.

These are native host observations from fresh Codex processes and real workers and lifecycle hooks, not synthesized local tests. Later phases, an entirely cache-resolved phase run, and final human signoff remain unverified. The closed run must not be retroactively accepted; any future attempt requires genuine authority under the intended budget policy.

## Evidence

Repair diagnostics: `/Users/vdemkiv/Documents/taskPlane/.taskplane/diagnostics/live-repair-2026-09-15-01a0a595`.

- `source-validation-summary.json` and the individual test logs describe partitioned source checks.
- `preserved-native-counter-recheck.json` records the read-only original usage check.
- `original-evidence-integrity-final.json` and `source-preservation-final.json` record preservation checks.
- `first-native-result.json`, `first-native-observation.md`, and `first-native-evidence/` preserve the first repaired run.
- `native-roles/installed-package-verification.json` verifies the current installed build.
- `native-roles/independent-audit.json` independently checks final native routing, Stop hashes, usage, and cleanup.
- `native-roles/source-installed-runtime-equivalence.json` records the qualified 170-file comparison, including the official hook-manifest projection.
- `native-roles/fixture-observation.md` and `fixture-evidence/` preserve the final fixture report and evidence.
- `native-roles/installed-launcher-smoke/` preserves the separate fresh-process cache-path smoke check.
- `native-roles/native-delivery-events.jsonl`, `native-delivery-process.json`, and the copied fixture evidence retain the final real execution.

## Remaining decision

The runtime repair is reviewable and installed. Completing the original full native delivery test still requires resolving the mismatch between measured native usage and the configured phase ceiling. Raising that ceiling or changing what counts toward it would change the existing budget contract, so neither was done. No implementation, full-delivery completion, performance improvement, or monetary-cost reduction is claimed from these partial runs.
