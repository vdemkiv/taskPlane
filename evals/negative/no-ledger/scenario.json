{
 "schema": "taskplane.eval-scenario/v1",
 "skill": "fixture",
 "steps": [
  {
   "id": "N1",
   "claim": "no derivation was repeated",
   "record": "derivations",
   "check": "repeats",
   "select": {
    "event": "derived",
    "probe": {
     "absent": true
    }
   },
   "distinct_by": [
    "key",
    "input_key"
   ],
   "max": 0,
   "universal": [
    "no_rederive"
   ],
   "required": true
  },
  {
   "id": "C1",
   "claim": "the contract was active before the run first touched the workspace \u2014 the control row, so a red fixture is red for the reason it names",
   "record": "trace",
   "check": "before",
   "select": {
    "event": "contract_activated"
   },
   "before": "first_write",
   "universal": [
    "contract"
   ],
   "required": true
  }
 ]
}
