{
  "note": "STATIC FIXTURE (t4, R-0004 resume): an execute-wave run killed mid-stage. The workflow runtime's journal replay reuses each completed agent's cached result WITHOUT re-dispatching it and re-runs every incomplete agent. Journal keying works ONLY because the stage workflow files label agents deterministically ('<prefix>:' + brief id, a pure literal rule) and contain no clock/randomness — test_resume_fixture binds this fixture to that rule programmatically.",
  "workflow": "execute-wave",
  "briefs": [
    {"id": "t1"},
    {"id": "t2"},
    {"id": "t3"}
  ],
  "journal": {
    "completed": [
      {
        "label": "task:t1",
        "cached": {
          "task": "t1",
          "outcome": "pass",
          "note": "built, committed and submitted before the kill"
        }
      }
    ],
    "killed_during": ["task:t2", "task:t3"]
  }
}
